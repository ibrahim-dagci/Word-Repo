import {
    StyleSheet
} from 'react-native';

export default StyleSheet.create({
    container: {
        alignItems: 'center',
        borderRadius: 15,
        height: 150,
    },
    record: {
        backgroundColor: 'white',
        justifyContent: 'center',
        alignItems: 'center',
        position: 'absolute',
        borderRadius: 30,
        bottom: 20,
        height: 60,
        width: 60,
    },
    red: {
        backgroundColor: '#E40202',
        borderRadius: 20,
        height: 40,
        width: 40,
    },
    status: {
        top: 20,
    },
});
