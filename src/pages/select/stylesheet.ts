import {
    StyleSheet
} from 'react-native';

export default StyleSheet.create({
    container: {
        width: '100%',
        flex: 1,
    },
    title: {
        fontWeight: 'bold',
        textAlign: 'center',
        width: '100%',
        fontSize: 25,
        padding: 5,
    },
});
